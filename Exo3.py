class Node:
    def __init__(self, val)
        self.left = None # is a Node
        self.right = None # is a Node
        self.value = val
    


# ABR : 
# left.val <= val <= righ.val
class Tree:
    def __init__(self, node)
        self.root = node
  
    def add(self.root, element):
       if self.root = None
            self.root.value := element
        
        else 
        {
            if self.root.value > element
                add(self.root.left, element)
                
            else
                add(self.root.right, element)
        }
        
    def run(Tree tree, val)
        if val in Tree
            return True
            
        else
            return False
        